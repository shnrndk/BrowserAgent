# Requirements for the evaluation pipeline
# Install with: pip install -r requirementsEval.txt

# Parquet file reading (benchmark datasets)
pandas>=1.5.0
pyarrow>=10.0.0

# JSONL trajectory file reading
jsonlines>=3.1.0

# LLM-judge evaluation (--use-llm flag)
# Provides the OpenAI API client used by val_answer_model_based.py
openai>=1.0.0

# .env file loading for OPENAI_API_KEY
python-dotenv>=1.0.0
